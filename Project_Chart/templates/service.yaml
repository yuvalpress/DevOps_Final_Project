apiVersion: v1
kind: Service
metadata:
  name: {{ include "Yuval_Press.fullname" . }}
  labels:
    {{- include "Yuval_Press.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5000
      protocol: TCP
      name: http
  selector:
    {{- include "Yuval_Press.selectorLabels" . | nindent 4 }}
